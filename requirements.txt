# 运行时依赖（保持最小）
requests>=2.31.0
Pillow>=9.0.0

# 二维码渲染（纯 Python，零依赖，约 60KB）
# 为什么需要：NapCat 的 GetQQLoginQrcode 返回的是「登录 URL」而不是图片，
# 官方前端在浏览器里把它渲染成二维码；我们要把二维码直接显示在自己的界面里，
# 就必须本地生成，不能依赖任何第三方二维码服务。
segno>=1.5.0

# 说明：
#  - WebDAV 上传只用 requests，不引入额外依赖
#  - OneBot11 客户端自实现（约 120 行），避免引入重量级框架
#  - 若日后想改用现成 SDK，可选的替代品（非必需）：
#      ncatbot        # NapCat 的 Python SDK
#      nonebot2       # 若要做完整机器人框架
