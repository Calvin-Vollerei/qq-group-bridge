# ===============================================================
# 凭据与配置：**绝不入库**
#
# 本工具会把网盘账号、QQ 令牌等加密后存在数据目录里。
# 这些文件一旦进了仓库就等于公开泄露，因此这里逐项排除。
# 发布前请再跑一次：python scripts/scan_secrets.py --source
# ===============================================================
secrets.enc
secrets.enc.tmp
*.enc
.env
.env.*
config.json
config.local.json
*.token
*.key
*.pem

# ---------------------------------------------------------------
# 运行时数据
# ---------------------------------------------------------------
state.db
state.db-wal
state.db-shm
logs/
tmp/
backup/
live_preview/
data/
portable.marker

# ---------------------------------------------------------------
# 本地开发 / 验证产物
# ---------------------------------------------------------------
.runtime/
.shots/
.docs/
.downloads/
napcat-start.log

# ---------------------------------------------------------------
# 第三方组件（体积大且含他人二进制，一律不入库）
#
# 本项目的原则是**不捆绑、不再分发** QQ 客户端与 NapCat，
# 由使用者在首次运行时自行下载。
#
# ⚠️ 这里必须写**带路径的完整模式**，不能只写目录名。
# 踩过的坑：早先写成 `napcat/`，Git 会匹配**任意层级**的同名目录，
# 于是把源码包 `qgb/napcat/` 也一起忽略了 —— 4 个源码文件从来没进过仓库，
# 本地测试全过、CI 与全新克隆全部 `ModuleNotFoundError: No module named 'qgb.napcat'`。
# 教训：忽略规则一律写成相对仓库根的完整路径，并在 tests/test_repo_hygiene.py 里回归。
# ---------------------------------------------------------------
data/napcat/
data/napcat-embedded/
OpenList/
runtime/

# ---------------------------------------------------------------
# Python
# ---------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
release/

# ---------------------------------------------------------------
# 工具
# ---------------------------------------------------------------
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea/
.vscode/
*.swp
Thumbs.db
desktop.ini
